# Ishaan Jhanji
# Honeycomb.py

import turtle

def hexagon():
    count = 0
    while count < 6:
        turtle.forward(50)
        turtle.right(60)
        count = count + 1
    count = 0
    while count < 6:
        turtle.forward(50)
        turtle.left(60)
        count = count + 1
    turtle.forward(50)
    turtle.left(60)
    turtle.forward(50)
    turtle.right(60)
    count = 0
    while count < 6:
        turtle.forward(50)
        turtle.right(60)
        count = count + 1
    count = 0
    while count < 6:
        turtle.forward(50)
        turtle.left(60)
        count = count + 1
    turtle.left(120)
    turtle.forward(50)
    turtle.left(60)
    count = 0
    while count < 6:
        turtle.forward(50)
        turtle.right(60)
        count = count + 1
    turtle.forward(50)
    turtle.left(60)
    turtle.forward(50)
    turtle.right(60)
    count = 0
    while count < 6:
        turtle.forward(50)
        turtle.left(60)
        count = count + 1
    count = 0
    while count < 6:
        turtle.forward(50)
        turtle.right(60)
        count = count + 1
        
        
    


hexagon()
turtle.done()

